# cx

A small C11 utility library focused on memory safety, custom allocators, and common containers.

## Features

* Custom allocator abstraction
* General-purpose and arena allocators
* Generic vectors
* Non-owning slices
* C11 with minimal dependencies

## Build

```bash
./build.sh
```

## Test

```bash
./build.sh test
```
